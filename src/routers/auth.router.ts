import { Request, Response, Router } from "express";

const router = Router({ caseSensitive: true });

// router.get("/", (req: Request) => passport.authenticate(req.params.from));

export default router;
